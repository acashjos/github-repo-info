== README

A simple Rails app to find Github repo's issue info from repo URL. Scraping using Nokogiri gem is used to get data about repo.

==== Possible improvements

Instead of doing the scrapping in the request cycle, do as background. Also, maintain an expiring cache of scraped results.
